# Requirements Archive: v1.0

**Archived:** 2026-05-17  
**Milestone:** v1.0 MVP — Signal System  
**Status:** All 44 v1 requirements satisfied and shipped  
**Coverage:** 44/44 requirements complete · 0 unmapped

---

## Already Validated (MVP Week 1)

The following requirements were complete before v1.0 work began.

- ✓ **MVP-01**: Operator can run `python -m signal_system daily-close` — existing
- ✓ **MVP-02**: System pings healthchecks.io /start, /success, /fail around every job — existing
- ✓ **MVP-03**: System stores signal and run records in SQLite with WAL mode — existing
- ✓ **MVP-04**: System sends email alerts via Gmail SMTP — existing
- ✓ **MVP-05**: System loads and validates all required environment variables at startup — existing
- ✓ **MVP-06**: System fetches daily market data via Finnhub free-tier API — existing
- ✓ **MVP-07**: Test suite covers DB init, signal insert, heartbeat, and job error paths — existing

---

## v1 Requirements — Final Status

### Shared Types

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| TYPE-01 | Canonical `Signal` dataclass in `models.py` — contract between agents and router | ✅ Shipped | Phase 1 | Frozen dataclass; Pydantic used for thesis taxonomy only |
| TYPE-02 | `alert_id` is SHA-256 of ticker + date + rule + agent (not UUID) — idempotent reruns | ✅ Shipped | Phase 1 | `compute_alert_id()` in `models.py`; `INSERT OR IGNORE` everywhere |

### Taxonomy

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| TAX-01 | Operator maintains thesis pillars in `thesis.yaml` without code changes | ✅ Shipped | Phase 1 | `thesis_loader.load_thesis()` reads at job start; Pydantic v2 validation |
| TAX-02 | System refuses to run News Classifier when `review_due` is past — hard abort, trips /fail | ✅ Shipped | Phase 1 | `ThesisStaleError(RuntimeError)` propagates through heartbeat context |
| TAX-03 | `thesis.yaml` loaded once per job start, validated against Pydantic schema before classification | ✅ Shipped | Phase 1 | `load_thesis()` called once in `news_morning.run()` before any Finnhub calls |
| TAX-04 | `thesis_version_hash` (SHA-256) stored on every classified signal row | ✅ Shipped | Phase 1 | Column on `signals` table; passed through from `load_thesis()` |

### Universe

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| UNIV-01 | Static ticker universe ~1,500 tickers with `core_holding` flag | ✅ Shipped | Phase 1 | `data/universe.csv` with `core_holding` column |
| UNIV-02 | Daily rotation via deterministic `hashlib.md5(ticker)` partitioning | ✅ Shipped | Phase 1 | `hashlib.md5` (not `hash()` which is process-salted) |
| UNIV-03 | Core holdings scanned every day regardless of partition | ✅ Shipped | Phase 1 | `get_core_holdings()` called separately; `get_todays_universe()` always includes core holdings |
| UNIV-04 | K-1 ETFs (USO, UNG, DBC, GSG) excluded at universe-builder level | ✅ Shipped | Phase 1 | Hard-coded exclusion set in `universe.py` |

### Schema

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| SCHEMA-01 | `signals.routing_status` column (DELIVERED/MONITORING/SUPPRESSED) — router sets, never modifies severity | ✅ Shipped | Phase 1 | Idempotent `_ensure_column()` migration in `init_db()` |
| SCHEMA-02 | `signals.signal_price_snapshot` column for unadjusted price at generation time | ✅ Shipped | Phase 1 | Column present; populated by agents |
| SCHEMA-03 | `signals.model_version` column storing pinned Claude model ID | ✅ Shipped | Phase 1 | Column present; populated by news classifier |
| SCHEMA-04 | `wash_sale` table with `account` column from day one (4 accounts) | ✅ Shipped | Phase 1 | Table created in `init_db()`; accounts: schwab_main, schwab_secondary, roth_ira, hsa |
| SCHEMA-05 | `llm_calls` table logging all four token counts per invocation | ✅ Shipped | Phase 1 | `insert_llm_call()` in `repository.py`; called by news classifier |
| SCHEMA-06 | `repository.count_delivered_today()` — router reads this, no in-memory alternatives | ✅ Shipped | Phase 1 | ET date-prefix LIKE query; called by `alert_router.py` at entry |

### Data Layer

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| DATA-01 | Finnhub client bulk quote fetch with preemptive rate-limit token bucket (≤55 calls/min) | ✅ Shipped | Phase 2 | Token bucket in `finnhub_client.py`; conservative buffer below 60/min |
| DATA-02 | Retry Finnhub 429 with exponential backoff via `tenacity` (up to 5 attempts) | ✅ Shipped | Phase 2 | `_RETRY_DECORATOR` on raw fetch functions |
| DATA-03 | Detect paid-tier endpoints (403/404), skip gracefully with logged warning | ✅ Shipped | Phase 2 | `PAID_TIER_STATUS_CODES` check; returns `None`/`[]`, no retry |
| DATA-04 | Fetch company news headlines for ticker within date range | ✅ Shipped | Phase 2 | `fetch_company_news(ticker, from_date, to_date)` in `finnhub_client.py` |

### News Classifier

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| CLFY-01 | Fetch, sanitize (strip control chars, cap 500 chars, `<headline>` delimiters), classify against thesis pillars | ✅ Shipped | Phase 3 | `_sanitize_headline()` in `news_classifier.py` |
| CLFY-02 | Anthropic `messages.parse()` with typed schema; `temperature=0`; pinned model ID | ✅ Shipped | Phase 3 | SDK-native structured output; no `instructor` |
| CLFY-03 | `thesis.yaml` system prompt with `cache_control: {type: "ephemeral"}` for prompt caching | ✅ Shipped | Phase 3 | `cache_read_input_tokens` logged to `llm_calls` table |
| CLFY-04 | On parse failure: insert MONITORING signal with `raw_response` — never silently drop | ✅ Shipped | Phase 3 | `_persist_monitoring_signal()` bypasses router directly |
| CLFY-05 | Classifier emits `Signal` objects; never sends email directly | ✅ Shipped | Phase 3 | `classify_headlines()` returns `list[Signal]`; caller handles routing |
| CLFY-06 | Headline deduplication within trading day prevents re-classifying same story | ✅ Shipped | Phase 3 | `headline_dedup_key()` (public API); two-layer: in-memory + DB `alert_id` |

### Discovery Agent

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| DISC-01 | Score tickers across 4 factors (weights default 35/30/25/10) using Finnhub free-tier endpoints | ✅ Shipped | Phase 4 | `score_universe()` in `discovery_agent.py`; per-factor sub-scores retained on Signal |
| DISC-02 | Tickers with missing required data: no signal (not artificially low score) | ✅ Shipped | Phase 4 | Score-floor guard; missing field → skip ticker entirely |
| DISC-03 | Phase A (logs-only) when `DISCOVERY_PHASE=A`; Phase B (live routing) on `B` — no code change | ✅ Shipped | Phase 4 | Dual-check in `discovery_agent.py` + `discovery.py` |
| DISC-04 | Emits `Signal` objects with per-factor sub-scores; never sends email directly | ✅ Shipped | Phase 4 | `score_universe()` returns `list[Signal]` in Phase B; logs MONITORING in Phase A |
| DISC-05 | Logs scanned ticker set per run to `runs` table | ✅ Shipped | Phase 4 | `update_run_counts()` called after scoring |

### Alert Router

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| ROUT-01 | Hard caps: 1 ACTION_REQUIRED and 3 INFORMATIONAL per day | ✅ Shipped | Phase 5 | `_BUDGET_AR=1`, `_BUDGET_INFO=3` module-level constants |
| ROUT-02 | Higher-scoring signal wins slot; loser written with `routing_status=SUPPRESSED` and `demoted_from` reason code — severity never mutated | ✅ Shipped | Phase 5 | `demoted_from` reason codes in `alert_router.py` |
| ROUT-03 | Router reads today's count from DB (`count_delivered_today()`), not memory | ✅ Shipped | Phase 5 | Called at router entry; safe for multi-job same-day runs |
| ROUT-04 | Budget reset at `America/New_York` midnight — never UTC | ✅ Shipped | Phase 5 | `datetime.now(ZoneInfo("America/New_York")).date()` prefix query |
| ROUT-05 | Deterministic tiebreak: alphabetical ticker as secondary sort — reruns produce identical decisions | ✅ Shipped | Phase 5 | `(-score, ticker)` sort tuple |

### Job Orchestration

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| JOBS-01 | `python -m signal_system news-morning` runs end-to-end inside heartbeat: fetch → classify → route → digest email | ✅ Shipped | Phase 6 | Registered in `__main__.py` JOBS dict |
| JOBS-02 | `python -m signal_system discovery` runs end-to-end inside heartbeat: universe → score → route/log | ✅ Shipped | Phase 6 | Registered in `__main__.py` JOBS dict |
| JOBS-03 | Zero-alert digest always sent: "Scanned N tickers, 0 alerts" — never silence | ✅ Shipped | Phase 6 | `validate_digest_payload()` raises if zero-alert line missing |
| JOBS-04 | news-morning hard-caps at 50 headlines; excess written as MONITORING with "volume cap reached" note | ✅ Shipped | Phase 6 | `_MAX_NEWS_HEADLINES=50`; overflow signals via `_make_volume_cap_signal()` |

### Measurement

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| MEAS-01 | `signals` table has `acted`, `acted_at`, `user_note` columns — filled manually within 7 days | ✅ Shipped | Phase 6 | Columns in schema; operator fills directly in SQLite |
| MEAS-02 | Outcome backfill job idempotently fills `outcome_price_30d`/`outcome_price_90d` — deferred ~30 days post go-live | ✅ Shipped (deferred) | Phase 6 | `outcome_backfill.py` exists, tested, but NOT registered in JOBS dict |

### Operations

| ID | Requirement | Status | Phase | Outcome |
|----|-------------|--------|-------|---------|
| OPS-01 | Windows Task Scheduler docs with exported `.xml` task file — absolute paths, missed-run policy, ET trigger, single-instance enforcement | ✅ Shipped | Phase 6 | `ops/task-scheduler-reference.xml` + `ops/windows-task-scheduler.md` |
| OPS-02 | Setup guide documents Gmail filter + healthchecks.io SMS/push configuration | ✅ Shipped | Phase 6 | `ops/operator-setup-checklist.md` |

---

## v2 Requirements (Deferred)

These were explicitly deferred during v1.0 and carry forward to the next milestone.

### Measurement (Long Horizon)

- **MEAS-V2-01**: Information Coefficient (Spearman rank correlation, score vs 30d outcome) tracked per agent type — deferred until ~30 days of live signal data
- **MEAS-V2-02**: Hit-rate vs base-rate comparison per signal type — deferred until sufficient sample size
- **MEAS-V2-03**: Score normalization to percentile rank within scanned universe — deferred until Phase A calibration data
- **MEAS-V2-04**: Weight version stamp on Discovery signals for IC interpretability after weight changes

### Operational

- **OPS-V2-01**: SQLite WAL checkpoint + weekly VACUUM + rolling weekly backup script — deferred to post-go-live hardening
- **OPS-V2-02**: `ticker_delisted_at` column in universe table with outcome backfill as of delisting date
- **OPS-V2-03**: Quarterly review tooling: query unannotated signals older than 7 days, prompt annotation or "expired" mark

### Signal Quality

- **QUAL-V2-01**: Source quality tier/whitelist for Finnhub news sources — deferred until baseline quality is measured
- **QUAL-V2-02**: Pillar delta vs absolute-level distinction in News Classifier — alert when trend changes direction

---

## Out of Scope (unchanged)

| Feature | Reason |
|---------|--------|
| Automated trade execution | System is alert-only by design |
| Self-learning / adaptive scoring | Operator adapts weights manually at quarterly review |
| Earnings Setup agent | Covered natively by Schwab |
| Portfolio Drift agent | Covered natively by Schwab |
| Regime classifier | Subsumed into news classifier pillar deltas |
| GitHub Actions runner | Windows Task Scheduler only |
| `^GSPC`, `^VIX`, insider sentiment | May be Finnhub paid-only; validate before use |
| Real-time intraday tick streaming | Daily-batch system |
| Multi-user / multi-tenant | Solo-operator system |
| UI dashboard | Email delivery is the interface |
| `asyncio` / `aiohttp` | Windows event-loop quirks |
| ORM (SQLAlchemy, peewee) | `repository.py` is the access layer |
| LLM frameworks (LangChain, CrewAI, AutoGen) | Conflict with heartbeat/dispatcher design |

---

## Traceability Summary

| Phase | Requirements | Count | All Complete |
|-------|-------------|-------|-------------|
| Phase 1 — Foundation | TYPE-01..SCHEMA-06 | 16 | ✅ |
| Phase 2 — Data Layer | DATA-01..DATA-04 | 4 | ✅ |
| Phase 3 — News Classifier | CLFY-01..CLFY-06 | 6 | ✅ |
| Phase 4 — Discovery Agent | DISC-01..DISC-05 | 5 | ✅ |
| Phase 5 — Alert Router | ROUT-01..ROUT-05 | 5 | ✅ |
| Phase 6 — Job Orchestration | JOBS-01..OPS-02 | 8 | ✅ |
| **Total** | | **44** | **44/44** |

---

*Requirements defined: 2026-05-14*  
*Archived: 2026-05-17 at v1.0 milestone close*
