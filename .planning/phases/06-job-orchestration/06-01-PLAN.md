---
plan_id: "06-01"
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
objective: "Build the shared orchestration primitives and the `news-morning` job: core-holdings lookup, previous-market-close boundary derivation, shared digest/persistence helpers, dispatcher wiring, and focused orchestration tests."
files_modified:
  - src/signal_system/data/universe.py
  - src/signal_system/state/repository.py
  - src/signal_system/jobs/common.py
  - src/signal_system/jobs/news_morning.py
  - src/signal_system/__main__.py
  - tests/test_job_orchestration.py
requirements: [JOBS-01, JOBS-03, JOBS-04]
tags: [jobs, news-morning, digest, router, previous-close, tests]

must_haves:
  truths:
    - "`news-morning` is a registered job in `__main__.py` and follows the repository/heartbeat/update-run lifecycle used by `daily_close.py`"
    - "The previous-close boundary is derived from the most recent successful `daily-close` run date at 4:00 PM ET, not from naïve date subtraction and not from raw `started_at` timestamp"
    - "If there is no successful prior `daily-close`, `news-morning` fails closed before fetching news or sending email"
    - "`load_thesis()` is called exactly once per `news-morning` run; stale or missing thesis aborts the run before any classification or digest send"
    - "Headline dedup happens before the 50-headline cap; only the newest 50 are classified"
    - "Each skipped headline beyond the cap is written as its own `MONITORING` row with a volume-cap note"
    - "Live digest completeness is enforced: if the digest is missing, duplicated, or its counts diverge from persisted routing outcomes, the run fails instead of silently succeeding"
    - "Successful runs send exactly one digest email; delivered alerts are detailed, non-delivered outcomes are count-only, and zero-alert days say `Scanned N tickers, 0 alerts`"
  artifacts:
    - path: "src/signal_system/jobs/common.py"
      provides: "shared digest rendering and routed tuple persistence"
    - path: "src/signal_system/jobs/news_morning.py"
      provides: "runnable news-morning job"
      exports: ["run"]
    - path: "tests/test_job_orchestration.py"
      provides: "Phase 6 orchestration coverage for helpers, news-morning, headline cap, and dispatcher wiring"
---

# Phase 6 / Plan 01 — Shared orchestration + `news-morning`

## Requirements Coverage

| Requirement | Covered By |
|-------------|------------|
| JOBS-01 | Wave 0 Task 1, Wave 0 Task 2, Wave 1 Task 1 |
| JOBS-03 | Wave 0 Task 2, Wave 1 Task 1 |
| JOBS-04 | Wave 1 Task 1 |

---

## Threat Model

| Threat | Mitigation | Location |
|--------|------------|----------|
| Wrong morning window misses post-close headlines | Derive the boundary from the last successful `daily-close` **date** at 4:00 PM ET | `repository.py`, `news_morning.py` |
| Silent zero-alert days look like failure | Shared digest renderer always emits explicit zero-alert text | `jobs/common.py`, `news_morning.py` |
| Overflow headlines disappear | Persist one `MONITORING` row per skipped headline | `news_morning.py` |

---

## Tasks

### Wave 0 — Shared prerequisites

<task type="auto" tdd="true">
  <name>Wave 0 / Task 1: Add core-holdings lookup and previous-close date helper</name>
  <files>src/signal_system/data/universe.py, src/signal_system/state/repository.py, tests/test_job_orchestration.py</files>
  <behavior>
    - `get_core_holdings()` returns only `core_holding=1` tickers, in CSV order, with existing K-1 filtering preserved
    - `get_latest_successful_run_date("daily-close")` returns the newest successful ET date for that job, or `None`
    - Failed and running rows are ignored
  </behavior>
  <action>
    Add `get_core_holdings()` beside `get_todays_universe()`. Add `get_latest_successful_run_date(job: str) -> date | None` in `repository.py`. Create helper tests in `tests/test_job_orchestration.py`.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && uv run pytest tests/test_job_orchestration.py -k "core_holdings or latest_successful_run_date" -q</automated>
  </verify>
  <done>
    `get_core_holdings()` and `get_latest_successful_run_date()` exist, are importable, and their helper tests are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0 / Task 2: Create shared job helpers for digest rendering and routed persistence</name>
  <files>src/signal_system/jobs/common.py, tests/test_job_orchestration.py</files>
  <behavior>
    - Shared persistence helper stores every routed tuple through `repository.insert_signal(..., demoted_from=...)`
    - Shared digest renderer details only `DELIVERED` alerts, summarizes `SUPPRESSED` and `MONITORING` by count, and emits explicit zero-alert confirmation
  </behavior>
  <action>
    Create `src/signal_system/jobs/common.py` with helper(s) for routed persistence and plain-text digest subject/body generation. Add focused helper tests.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && uv run pytest tests/test_job_orchestration.py -k "shared_digest or routed_persistence" -q</automated>
  </verify>
  <done>
    Shared job helpers exist, preserve routed persistence metadata, and generate digest text that matches the locked delivery rules.
  </done>
</task>

### Wave 1 — `news-morning`

<task type="auto" tdd="true">
  <name>Wave 1 / Task 1: Build `src/signal_system/jobs/news_morning.py` and register `news-morning`</name>
  <files>src/signal_system/jobs/news_morning.py, src/signal_system/__main__.py, tests/test_job_orchestration.py</files>
  <behavior>
    - `news-morning` inserts its run row before heartbeat and marks success inside heartbeat
    - If no successful prior `daily-close` exists, the job raises before any Finnhub call or email send
    - `load_thesis()` runs exactly once; stale or missing thesis fails the job before classification or digest send
    - The job scans core holdings only by calling `get_core_holdings()`, not `get_todays_universe()`
    - The lower bound is the successful `daily-close` date at 4:00 PM ET
    - Headlines are deduped before the cap, sorted newest-first, capped at 50, and overflow items are logged as `MONITORING`
    - Classifier `MONITORING` outputs, including malformed structured-output recovery paths, bypass the router and are persisted directly
    - Routable signals flow through `route_signals()`
    - The job validates digest completeness before marking success: exactly one digest send, counts consistent with persisted routing outcomes, and zero-alert confirmation present when delivered count is zero
    - Exactly one digest email is sent on each successful run
  </behavior>
  <action>
    Implement `news_morning.py` using `daily_close.py` as the lifecycle template, explicitly use `get_core_holdings()` for the scan set, register the job in `__main__.py`, and add orchestration tests for core-holdings-only behavior, cold start, stale/missing thesis failure, zero-alert confirmation, cap ordering, overflow persistence, parse-failure `MONITORING` persistence, digest-vs-persistence count consistency, fail-on-mismatch, and dispatcher wiring.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && uv run pytest tests/test_job_orchestration.py -k "news_morning or core_holdings or headline_cap or zero_alert or dispatcher or thesis or parse_failure or digest_counts or digest_mismatch" -q</automated>
  </verify>
  <done>
    `python -m signal_system news-morning` is wired, tested, and preserves the core-holdings-only scope, thesis gate, fail-closed cold start, parse-failure `MONITORING` persistence, headline-cap handling, and digest-completeness guardrails.
  </done>
</task>

---

## Out of Scope

- Discovery job orchestration
- Outcome backfill
- Ops artifacts
- Milestone audit closeout
