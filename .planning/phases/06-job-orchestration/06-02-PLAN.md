---
plan_id: "06-02"
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
autonomous: true
objective: "Create the runnable `discovery` job on top of the existing Discovery Agent contract, preserving Phase A no-email behavior and Phase B digest/routing behavior."
files_modified:
  - src/signal_system/jobs/discovery.py
  - src/signal_system/__main__.py
  - tests/test_job_orchestration.py
requirements: [JOBS-02, JOBS-03]
tags: [jobs, discovery, phase-a, phase-b, digest, dispatcher]

must_haves:
  truths:
    - "`python -m signal_system discovery` is a registered job"
    - "The job branches on `config.DISCOVERY_PHASE`, not on the returned signal list"
    - "Phase A sends no email and makes no router call"
    - "Live Discovery Phase B enforces digest completeness: if the digest is missing, duplicated, or its counts diverge from persisted routing outcomes, the run fails"
    - "Phase B routes returned signals, persists all routing outcomes, and sends one digest even when nothing is delivered"
---

# Phase 6 / Plan 02 — `discovery`

## Requirements Coverage

| Requirement | Covered By |
|-------------|------------|
| JOBS-02 | Wave 2 Task 1 |
| JOBS-03 | Wave 2 Task 1 |

---

## Tasks

<task type="auto" tdd="true">
  <name>Wave 2 / Task 1: Build `src/signal_system/jobs/discovery.py`</name>
  <files>src/signal_system/jobs/discovery.py, src/signal_system/__main__.py, tests/test_job_orchestration.py</files>
  <behavior>
    - The job inserts its run row before heartbeat and marks success inside heartbeat
    - It always calls `get_todays_universe()` and `score_universe(tickers, run_id, date_iso)`
    - It branches on `config.DISCOVERY_PHASE`
    - Phase A stops after `score_universe()` and sends no email
    - Phase B routes returned signals, persists routed tuples, validates digest-vs-persistence count consistency, and sends one digest with explicit zero-alert confirmation
  </behavior>
  <action>
    Implement `discovery.py` with the `daily_close.py` lifecycle pattern, reuse the shared helpers from Plan 01, register the job in `__main__.py`, and extend `tests/test_job_orchestration.py` for Phase A and Phase B behavior, including digest completeness and fail-on-mismatch in Phase B.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && uv run pytest tests/test_job_orchestration.py -k "discovery_phase_a or discovery_phase_b or dispatcher or digest_counts or digest_mismatch" -q</automated>
  </verify>
  <done>
    `python -m signal_system discovery` is runnable, preserves the Discovery Agent phase contract, and enforces the same digest-completeness guardrails in Phase B.
  </done>
</task>

---

## Out of Scope

- Discovery scoring changes
- Measurement / ops artifacts
- Milestone audit closeout
