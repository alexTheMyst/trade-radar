---
plan_id: "06-03"
phase: "06"
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
autonomous: true
objective: "Add deferred measurement support and the Windows/operator handoff artifacts without widening the public runtime surface."
files_modified:
  - src/signal_system/jobs/outcome_backfill.py
  - src/signal_system/state/repository.py
  - tests/test_outcome_backfill.py
  - ops/task-scheduler-reference.xml
  - ops/windows-task-scheduler.md
  - ops/operator-setup-checklist.md
requirements: [MEAS-01, MEAS-02, OPS-01, OPS-02]
tags: [measurement, backfill, scheduler, ops, checklist]

must_haves:
  truths:
    - "Deferred outcome backfill exists as internal code only and is not added to `JOBS`"
    - "Backfill is idempotent and only fills 30d/90d outcomes after the corresponding threshold has passed"
    - "`ops/task-scheduler-reference.xml` is scrubbed and uses the chosen command style `uv run python -m signal_system <job>`"
    - "`ops/windows-task-scheduler.md` provides the dedicated prose setup guide for Windows scheduling"
    - "`ops/task-scheduler-reference.xml` and the checklist explicitly cover absolute paths, Eastern Time trigger, StartWhenAvailable, single-instance enforcement, and password-backed logon"
    - "`ops/operator-setup-checklist.md` documents Gmail filter setup, Healthchecks push/SMS setup, manual `daily-close` prerequisite, single-instance scheduler settings, and the 7-day operator feedback workflow using `acted`, `acted_at`, and `user_note`"
---

# Phase 6 / Plan 03 — Measurement + ops handoff

## Requirements Coverage

| Requirement | Covered By |
|-------------|------------|
| MEAS-01 | Wave 3 Task 2 |
| MEAS-02 | Wave 3 Task 1 |
| OPS-01 | Wave 3 Task 2 |
| OPS-02 | Wave 3 Task 2 |

---

## Tasks

<task type="auto" tdd="true">
  <name>Wave 3 / Task 1: Implement deferred internal outcome backfill</name>
  <files>src/signal_system/jobs/outcome_backfill.py, src/signal_system/state/repository.py, tests/test_outcome_backfill.py</files>
  <behavior>
    - Backfill logic is importable and callable internally, but is not registered in `__main__.py`
    - It selects rows with `acted IS NOT NULL` and missing outcome columns
    - It fills 30d/90d outcomes only after the corresponding threshold has passed
    - Re-runs do not overwrite already-filled outcome columns
  </behavior>
  <action>
    Add repository helper(s) to select eligible rows and persist outcome prices, create `jobs/outcome_backfill.py`, and add tests for deferred activation plus idempotence.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && uv run pytest tests/test_outcome_backfill.py -q</automated>
  </verify>
  <done>
    Deferred backfill exists as tested internal code only and remains out of the public dispatcher.
  </done>
</task>

<task type="auto">
  <name>Wave 3 / Task 2: Create scrubbed ops artifacts and operator checklist</name>
  <files>ops/task-scheduler-reference.xml, ops/windows-task-scheduler.md, ops/operator-setup-checklist.md</files>
  <behavior>
    - XML artifact is scrubbed and references the chosen `uv run python -m signal_system <job>` command shape
    - Dedicated scheduler prose guide exists and, together with the XML artifact, explicitly covers absolute paths and an Eastern Time trigger
    - Checklist covers Gmail filter, Healthchecks non-email alerts, `StartWhenAvailable`, password-backed logon, single-instance enforcement, and the manual `daily-close` prerequisite for `news-morning`
    - Operator-facing docs explicitly state that `MEAS-02` exists in code but remains inactive until ~30 days post-go-live
    - Manual operator feedback fields (`acted`, `acted_at`, `user_note`) remain part of the documented workflow and must be filled within 7 days of alert
  </behavior>
  <action>
    Create `ops/task-scheduler-reference.xml`, a dedicated prose guide at `ops/windows-task-scheduler.md`, and `ops/operator-setup-checklist.md` using the repo's existing checklist-heavy doc style. Do not include real usernames, secrets, or machine-specific paths. Explicitly document that deferred outcome backfill remains inactive until ~30 days post-go-live and that operator feedback fields must be annotated within 7 days of alert.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/code/trading_agent && test -f ops/task-scheduler-reference.xml && test -f ops/windows-task-scheduler.md && test -f ops/operator-setup-checklist.md && rg -n "uv run python -m signal_system|StartWhenAvailable|single-instance|absolute path|Eastern Time|run whether logged on or not|password-backed" ops/task-scheduler-reference.xml ops/windows-task-scheduler.md && rg -n "Gmail filter|Healthchecks|30 days post-go-live|7 days|acted|acted_at|user_note" ops/windows-task-scheduler.md ops/operator-setup-checklist.md</automated>
  </verify>
  <done>
    The scrubbed XML, dedicated scheduler guide, and operator checklist fully cover OPS-01 and OPS-02, including absolute paths, ET trigger guidance, the deferred-activation message for `MEAS-02`, and the 7-day MEAS-01 workflow.
  </done>
</task>

---

## Out of Scope

- Exposing backfill as a public CLI job
- Solving rerun-email idempotence
- Exact historical-close backfill semantics beyond current requirement scope
